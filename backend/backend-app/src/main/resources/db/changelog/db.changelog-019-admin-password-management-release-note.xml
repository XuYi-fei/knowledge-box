<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="019-001-about-release-note-admin-password-management" author="codex">
        <sql><![CDATA[
            INSERT INTO about_release_note (
                version_label,
                title,
                summary,
                content_markdown,
                published_at,
                display_order,
                highlighted
            )
            SELECT
                '2026.03.12',
                '管理端支持在线修改密码',
                '管理员登录后可直接校验旧密码并更新新密码，更新后立即生效且不会被启动初始化覆盖。',
                E'## 本次更新\n\n- 新增 `POST /api/admin/me/password` 管理员改密接口\n- 改密流程要求提交当前密码与新密码，避免误改\n- `knowledge-box.admin.password` 仅用于初始化缺失密码，不会覆盖已改过的数据库密码',
                TIMESTAMPTZ '2026-03-12 23:40:00+08',
                -1,
                FALSE
            WHERE NOT EXISTS (
                SELECT 1
                FROM about_release_note
                WHERE version_label = '2026.03.12'
                  AND title = '管理端支持在线修改密码'
            );
        ]]></sql>
    </changeSet>
</databaseChangeLog>

<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="028-001-trace-dual-view-release-note" author="codex">
        <sql><![CDATA[
            INSERT INTO about_release_note (
                version_label,
                title,
                summary,
                content_markdown,
                published_at,
                display_order,
                highlighted
            )
            SELECT
                '2026.03.15',
                '管理员 Trace 支持双层分析视图',
                'Trace 详情新增 Agent 时间线与后端调用瀑布，便于同时理解 ReActAgent 执行过程与服务方法耗时。',
                E'## 本次更新\n\n- Trace 详情页新增 `Agent 时间线` 视图，按严格时间顺序展示接收请求、Prompt 注入、工具调用、Agent 结果与最终响应\n- Trace 详情页新增 `后端调用瀑布` 视图，展示聊天链路关键服务方法的父子关系、开始时间与耗时\n- 原始 trace/span/event 日志仍保留在 `原始日志` 视图中，便于高级排障',
                TIMESTAMPTZ '2026-03-15 10:30:00+08',
                -3,
                FALSE
            WHERE NOT EXISTS (
                SELECT 1
                  FROM about_release_note
                 WHERE version_label = '2026.03.15'
                   AND title = '管理员 Trace 支持双层分析视图'
            );
        ]]></sql>
    </changeSet>
</databaseChangeLog>
